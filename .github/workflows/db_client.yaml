# ABOUTME: CI workflow for the db_client package.
# ABOUTME: Runs tests, formatting, and analysis only when db_client files change.

name: DB Client CI

on:
  push:
    branches: [main]
    paths:
      - "mobile/packages/db_client/**"
      - ".github/workflows/db_client.yaml"
  pull_request:
    branches: [main]
    paths:
      - "mobile/packages/db_client/**"
      - ".github/workflows/db_client.yaml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: DB Client CI
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile/packages/db_client/

    steps:
      - uses: actions/checkout@v4

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.38.3"
          channel: "stable"
          cache: true

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: ✨ Check formatting
        run: dart format --output=none --set-exit-if-changed lib test

      - name: 🕵️ Analyze code
        run: flutter analyze lib test

      - name: 🧪 Run tests
        run: flutter test
